@extends('layouts.app') @section('content')<h1>Routers</h1><a href="{{ route('routers.create') }}">Add Router</a><table><tr><th>Name</th><th>Host</th><th>Status</th><th>Actions</th></tr>@foreach($routers as $router)<tr><td><a href="{{ route('routers.show',$router) }}">{{ $router->name }}</a></td><td>{{ $router->host }}</td><td>{{ $router->status }}</td><td><a href="{{ route('routers.edit',$router) }}">Edit</a><form method="post" action="{{ route('routers.destroy',$router) }}" style="display:inline">@csrf @method('DELETE')<button>Delete</button></form><form method="post" action="{{ route('routers.test',$router) }}" style="display:inline">@csrf<button>Test</button></form></td></tr>@endforeach</table>@endsection