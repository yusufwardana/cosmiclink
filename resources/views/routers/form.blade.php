@extends('layouts.app') @section('content')<h1>{{ $router->exists ? 'Edit' : 'Add' }} Router</h1><form method="post" action="{{ $router->exists ? route('routers.update',$router) : route('routers.store') }}">@csrf @if($router->exists) @method('PUT') @endif<label>Name<input name="name" value="{{ old('name',$router->name) }}" required></label><label>Description<textarea name="description">{{ old('description',$router->description) }}</textarea></label><label>Host<input name="host" value="{{ old('host',$router->host) }}" required></label><label>API port<input name="api_port" type="number" value="{{ old('api_port',$router->api_port ?: 8728) }}" required></label><label>Username<input name="username" value="{{ old('username',$router->username) }}" required></label><label>Password<input name="password" type="password" autocomplete="new-password"></label><button>Save</button></form>@endsection