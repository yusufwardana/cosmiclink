@extends('layouts.app') @section('content')<h1>{{ $customer->exists?'Edit':'Add' }} Customer</h1><form method="post" action="{{ $customer->exists?route('customers.update',$customer):route('customers.store') }}">@csrf @if($customer->exists) @method('PUT') @endif<label>Name<input name="name" value="{{ old('name',$customer->name) }}" required></label><label>Phone<input name="phone" value="{{ old('phone',$customer->phone) }}"></label><label>Email<input name="email" type="email" value="{{ old('email',$customer->email) }}"></label><label>Address<textarea name="address">{{ old('address',$customer->address) }}</textarea></label><label>Notes<textarea name="notes">{{ old('notes',$customer->notes) }}</textarea></label><label>Status<select name="status"><option value="active" @selected(old('status',$customer->status)==='active')>Active</option><option value="inactive" @selected(old('status',$customer->status)==='inactive')>Inactive</option></select></label><button>Save</button></form>@endsection