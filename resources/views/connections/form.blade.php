@extends('layouts.app') @section('content')<h1>Add Connection for {{ $customer->name }}</h1><form method="post" action="{{ route('customers.connections.store',$customer) }}">@csrf<label>Internet Package<select name="internet_package_id" required><option value="">Select package</option>@foreach($packages as $package)<option value="{{ $package->id }}">{{ $package->name }} — {{ $package->download_mbps }}/{{ $package->upload_mbps }} Mbps</option>@endforeach</select></label><label>Router<select name="router_id" required><option value="">Select router</option>@foreach($routers as $router)<option value="{{ $router->id }}">{{ $router->name }}</option>@endforeach</select></label><button>Create Connection</button></form>@endsection